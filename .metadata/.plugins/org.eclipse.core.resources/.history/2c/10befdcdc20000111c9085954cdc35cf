<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
  <meta charset="UTF-8">
  <title>Add Patient</title>
  <style>
    body { font-family: Segoe UI, sans-serif; background: #f4f7f6; padding: 24px; }
    .container { background: #fff; width: 560px; margin: auto; padding: 26px; border-radius: 8px; box-shadow: 0 4px 8px rgba(0,0,0,.08); }
    .row { margin-bottom: 12px; }
    label { display:block; color:#2c3e50; margin-bottom:6px; }
    input { width:100%; padding:10px; border:1px solid #ddd; border-radius: 4px; }
    .btn { margin-top: 12px; padding: 10px 14px; border:none; background:#27ae60; color:#fff; border-radius:4px; cursor:pointer; }
    a { margin-left: 8px; }
  </style>
</head>
<body>
<div class="container">
  <h2>Add Patient</h2>
  <form th:action="@{/admin/patients/save}" th:object="${patient}" method="post">
    <div class="row">
      <label>Full Name</label>
      <input type="text" th:field="*{fullName}" required />
    </div>
    <div class="row">
      <label>Gender</label>
      <input type="text" th:field="*{gender}" />
    </div>
    <div class="row">
      <label>Contact Number</label>
      <input type="text" th:field="*{contactNumber}" />
    </div>
    <div class="row">
      <label>Email</label>
      <input type="email" th:field="*{email}" />
    </div>
    <div class="row">
      <label>Address</label>
      <input type="text" th:field="*{address}" />
    </div>
    <div class="row">
      <label>Blood Group</label>
      <input type="text" th:field="*{bloodGroup}" />
    </div>
    <button class="btn" type="submit">Save</button>
    <a th:href="@{/admin/patients}">Cancel</a>
  </form>
</div>
</body>
</html>