<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
  <meta charset="UTF-8">
  <title>Manage Patients - Admin Panel</title>
  <style>
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #f4f7f6; margin: 0; padding: 20px; }
    .container { max-width: 1100px; margin: auto; background: white; padding: 30px; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
    h2 { color: #2c3e50; border-bottom: 2px solid #5dade2; padding-bottom: 10px; margin-top: 0; }
    .actions { display: flex; gap: 10px; align-items: center; margin-top: 10px; }
    .btn { padding: 8px 15px; text-decoration: none; border-radius: 4px; font-size: 14px; cursor: pointer; border: none; }
    .btn-add { background-color: #27ae60; color: white; }
    .btn-edit { background-color: #2980b9; color: white; margin-right: 6px; }
    .btn-delete { background-color: #c0392b; color: white; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
    th { background-color: #f8f9fa; color: #34495e; }
    tr:hover { background-color: #f5f9ff; }
    input[type="text"] { padding: 8px; width: 260px; }
    a { text-decoration: none; }
  </style>
</head>
<body>

<div class="container">
  <h2>Manage Patients</h2>

  <div class="actions">
    <form th:action="@{/admin/patients}" method="get" style="display:flex; gap:8px; align-items:center;">
      <input type="text" name="q" th:value="${q}" placeholder="Search by name..." />
      <button class="btn btn-edit" type="submit">Search</button>
      <a th:href="@{/admin/patients}" class="btn">Clear</a>
    </form>

    <a th:href="@{/admin/patients/add}" class="btn btn-add">Add New Patient</a>
  </div>

  <table>
    <thead>
    <tr>
      <th>ID</th>
      <th>Full Name</th>
      <th>Gender</th>
      <th>Contact</th>
      <th>Email</th>
      <th>Blood Group</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <tr th:each="p : ${patients}">
      <td th:text="${p.id}">1</td>
      <td th:text="${p.fullName}">John Doe</td>
      <td th:text="${p.gender}">F</td>
      <td th:text="${p.contactNumber}">99999</td>
      <td th:text="${p.email}">john@demo.com</td>
      <td th:text="${p.bloodGroup}">O+</td>
      <td>
        <a th:href="@{/admin/patients/edit/{id}(id=${p.id})}" class="btn btn-edit">Edit</a>

        <!-- ✅ Delete (POST) with confirm dialog -->
        <form th:action="@{/admin/patients/delete/{id}(id=${p.id})}"
              method="post"
              style="display:inline;">
          <!-- If you enable CSRF later, un-comment the next line -->
          <!-- <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/> -->

          
           
          </button>
        </form>
      </td>
    </tr>

    <tr th:if="${#lists.isEmpty(patients)}">
      <td colspan="7">No patients found.</td>
    </tr>
    </tbody>
  </table>
</div>

</body>
</html>
``